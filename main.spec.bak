# -*- mode: python ; coding: utf-8 -*-


import sys, os
block_cipher = None
dir_path = os.path.sep.join(sys.argv[0].split(os.path.sep)[:-1])



a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[(str(dir_path)+ '/.Dialogue.txt', '.'), (str(dir_path) + '/.French Words.txt', '.'), (str(dir_path) + '/.left_right_choices.txt', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)
pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='main',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
